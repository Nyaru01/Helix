<ui:FluentWindow x:Class="LocalBlast.Windows.AlignmentWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="BLAST alignment"
                 Width="1080"
                 Height="720"
                 MinWidth="760"
                 MinHeight="500"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="AlignmentTitleBar" Title="BLAST alignment" />

        <ui:Card Grid.Row="1" Margin="20,14,20,12" Padding="16">
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" />
        </ui:Card>

        <TextBox x:Name="AlignmentTextBox"
                 Grid.Row="2"
                 Margin="20,0,20,12"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Cascadia Mono, Consolas"
                 FontSize="13" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <ui:Button Content="Close" Click="Close_Click" MinWidth="100" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
