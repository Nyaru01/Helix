<ui:FluentWindow x:Class="LocalBlast.Windows.ResultsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Helix Blast results"
                 Width="1320"
                 Height="760"
                 MinWidth="900"
                 MinHeight="560"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="ResultsTitleBar" Title="BLAST results" />

        <ui:Card Grid.Row="1" Margin="20,14,20,12" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="SummaryText" FontSize="17" FontWeight="SemiBold" />
                    <TextBlock Text="Double-click a row to inspect the Query / Sbjct alignment." Margin="0,4,0,0" Opacity="0.6" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="ThresholdText" VerticalAlignment="Center" Opacity="0.65" Margin="0,0,12,0" />
                    <ui:Button Content="Export CSV" Click="ExportCsv_Click" />
                    <ui:Button Content="Export HTML" Click="ExportHtml_Click" Margin="8,0,0,0" />
                    <ui:Button Content="Export PDF" Click="ExportPdf_Click" Margin="8,0,0,0" />
                </StackPanel>
            </Grid>
        </ui:Card>

        <Grid Grid.Row="2" Margin="20,0,20,20">
            <Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions><ColumnDefinition Width="280"/><ColumnDefinition Width="12"/><ColumnDefinition Width="170"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBox x:Name="SearchTextBox" ToolTip="Search sample, contig or file" TextChanged="FilterChanged" />
                <ComboBox x:Name="StatusFilterComboBox" Grid.Column="2" SelectionChanged="FilterChanged"><ComboBoxItem Content="All statuses" IsSelected="True"/><ComboBoxItem Content="Present"/><ComboBoxItem Content="Below thresholds"/><ComboBoxItem Content="No hit"/><ComboBoxItem Content="Error"/></ComboBox>
            </Grid>
            <Border Grid.Row="1" CornerRadius="10" ClipToBounds="True">
                <DataGrid x:Name="ResultsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      MouseDoubleClick="ResultsGrid_MouseDoubleClick"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Genome / file" Binding="{Binding GenomeName}" Width="150" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="135" />
                    <DataGridTextColumn Header="Identity" Binding="{Binding IdentityDisplay}" Width="90" />
                    <DataGridTextColumn Header="Coverage" Binding="{Binding CoverageDisplay}" Width="90" />
                    <DataGridTextColumn Header="Subject / contig" Binding="{Binding SubjectId}" Width="*" MinWidth="220" />
                    <DataGridTextColumn Header="Coordinates" Binding="{Binding CoordinatesDisplay}" Width="130" />
                    <DataGridTextColumn Header="E-value" Binding="{Binding EvalueDisplay}" Width="100" />
                    <DataGridTextColumn Header="Bit score" Binding="{Binding BitScoreDisplay}" Width="90" />
                    <DataGridTextColumn Header="File" Binding="{Binding GenomeFile}" Width="2*" MinWidth="280" />
                </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
